# Аудит ссылок в разделе советов

Дата проверки: 2026-07-07.

Повторная проверка после обновления каталога `/sovety/` показала, что ранее найденный пробел закрыт: карточка для страницы `/sovety/pochemu-lak-na-parkete-pyatnami-posle-lakirovki/` добавлена в блок `После лакировки`.

Issue #271 закрыт PR #273.
Issue #274 закрыт последующими правками каталога советов.

## Результат

- `/sovety/pochemu-lak-na-parkete-pyatnami-posle-lakirovki/` — карточка есть в `sovety/index.html`.
- Ручной отчёт по новым расхождениям нужно запускать командой `python tools/report_advice_index_gaps.py`.

## Что проверять дальше

После добавления новых статей в `/sovety/` нужно синхронно обновлять:

1. саму страницу статьи;
2. запись в `sitemap.xml`;
3. карточку в `sovety/index.html`;
4. дату `lastmod` у раздела `/sovety/` в `sitemap.xml`.
