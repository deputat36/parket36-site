# Live-контракт звонка, форм и собранной главной

Дата обновления: 2026-07-24.

## Назначение

`tools/check_live_conversion.py` проверяет номер телефона, Fail-closed формы и признаки готовой GitHub Pages-сборки на `parket36.ru`. Проверка должна отличать опубликованный `_site` от сырого исходника, неполной сборки или промежуточного CDN-кэша.

Проверка входит в `.github/workflows/live-site-health.yml` и запускается:

- сразу после успешного `Deploy GitHub Pages`;
- ежедневно;
- вручную через `workflow_dispatch`.

## Телефонный маршрут

На боевой главной одновременно должны присутствовать:

- точный `href="tel:+79009267929"` из `data/site.json`;
- отображаемый номер из общих настроек;
- подпись `Позвонить Ивану`;
- действие `Оценка по фото`.

Видимый номер без рабочего `tel:` считается ошибкой.

## Fail-closed формы

Проверяются две реальные страницы:

- `/zayavka/` — подробная оценка по фото;
- `/kontakty/` — обратный звонок.

На каждой странице live HTML должен содержать:

- `data-form-safety="pending"` на форме;
- `data-form-safety-submit="true"` на submit-кнопке;
- отключённую кнопку с `disabled` и `aria-disabled="true"`;
- `data-form-no-script="true"` с честным сообщением, что данные не отправлены;
- прямой телефон Ивана;
- cache-busted скрипт `form-fail-closed`.

Эти признаки подтверждают, что при отключённом JavaScript форма не имитирует отправку, а при частичной загрузке первым подключается отдельный защитный модуль. Проверка только читает HTML и не отправляет заявки.

## Контракт готовой сборки

Главная должна содержать все маркеры, добавляемые только во время формирования `_site`:

- `<!-- shared-shell:header -->`;
- `<!-- shared-shell:final-cta -->`;
- `<!-- shared-shell:footer -->`;
- `<!-- shared-shell:mobile-cta -->`;
- `data-css-bundle="true"`.

Отсутствие хотя бы одного маркера означает, что домен может отдавать сырой исходник, неполную сборку или устаревший HTML. Такой результат делает live workflow красным даже при сохранённом номере телефона.

## IndexNow-ключ

Проверяется:

- HTTP 200 для публичного файла ключа;
- точное совпадение содержимого с `data/indexnow.json`;
- отсутствие вывода самого ключа в диагностический отчёт.

## Защита от CDN-кэша

Каждая попытка использует уникальные query-параметры:

- `verify_conversion` для главной;
- `verify_form_safety_1` и `verify_form_safety_2` для форм;
- `verify_indexnow_key` для файла ключа;
- `attempt` с номером попытки.

Запросы дополнительно отправляют:

```text
Cache-Control: no-cache, no-store, max-age=0
Pragma: no-cache
```

Post-deploy режим выполняет до шести попыток с интервалом 10 секунд. Ежедневный и ручной запуск выполняют одну попытку.

## Диагностика

Результаты дописываются в общий `live-health-report.md`. Для каждого HTTP-запроса указывается номер `cache_bust_attempt`, но случайный nonce и содержимое IndexNow-ключа в отчёт не выводятся.

Если проверка не прошла, artifact всё равно сохраняется. После повторного сбоя существующий механизм monitoring issue создаёт или обновляет задачу `[monitoring] parket36.ru live health failure`.

## Офлайн-проверки

```bash
python tools/check_live_conversion.py --self-test
python tools/check_live_conversion_workflow.py
```

Self-test проверяет рабочие и неполные телефонные маркеры, полный и повреждённый shared-shell контракт, полный и повреждённый Fail-closed контракт формы, точное содержимое ключа, cache-busted URL и ограничения runtime-параметров.

## Границы

Проверка ничего не публикует и не изменяет на боевом сайте. Она не отправляет заявки, не вызывает Supabase Edge Function и не подтверждает поисковую индексацию. Её задача — доказать, что live-домен отдаёт готовую конверсионную сборку, безопасные формы и публичный ключ владения IndexNow.
